#include <iostream>
#include <stack>
using namespace std;

int main() {
  // For the rest of this file, print out each value on a line by
  // itself. For example, if a comment says to print 5, you should
  // write:
  //   cout << 5 << endl;

  // Use the following page as a reference:
  // http://www.cplusplus.com/reference/stack/stack/

  // FΙХME: create a stack that holds ints
  // FΙХME: push 5 onto the stack
  // FΙХME: print the top of the stack
  // FΙХME: push 7 onto the stack
  // FΙХME: print the top of the stack
  // FΙХME: push 3 onto the stack
  // FΙХME: print the top of the stack
  // FΙХME: print the size of the stack
  // FΙХME: pop the stack
  // FΙХME: print the top of the stack
  // FΙХME: print the size of the stack
  // FΙХME: pop the stack
  // FΙХME: print the top of the stack
  // FΙХME: pop the stack
  // FΙХME: print the size of the stack


  return 0;
}